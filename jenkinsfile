pipeline {
    agent any
    environment {
        flutter = 'C:\\Users\\TBS\\fvm\\default\\bin\\flutter'
    }
    stages {
        stage('GIT PULL') {
            steps {
                git branch: 'master',
                url: 'https://github.com/lucas-castro-icts/android_jenkins.git',
                gitTool: 'Default'
            }
        }

        stage('CLEAN') {
            steps {
                sh "${flutter} clean"
            }
        }

        stage('PUB GET') {
            steps {
                sh "${flutter} pub get"
            }
        }
    }
}
