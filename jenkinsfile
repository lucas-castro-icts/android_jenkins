pipeline {
    agent any

    stages {
        stage('GIT PULL') {
            steps {
                git branch: 'master',
                url: 'https://github.com/lucas-castro-icts/android_jenkins.git'
            }
        }

        stage('CLEAN') {
            steps {
                sh 'flutter --version'
            }
        }
    }
}
