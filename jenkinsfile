pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/lucas-castro-icts/android_jenkins.git'
            }
        }

        stage('Build') {
            steps {
                bat 'flutter doctor -v'
            }
        }
    }
}
