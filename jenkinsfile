pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
                git 'https://github.com/lucas-castro-icts/android_jenkins'
            }
        }

        stage('Setup Flutter') {
            steps {
                // Verifica a instalação do Flutter
                sh 'flutter --version'
            }
        }

        // stage('Dependencies') {
        //     steps {
        //         sh 'flutter pub get'
        //     }
        // }

        // stage('Build Android') {
        //     steps {
        //         sh 'flutter build apk --release'
        //     }

        //     post {
        //         success {
        //             archiveArtifacts artifacts: 'build/app/outputs/flutter-apk/app-release.apk', fingerprint: true
        //         }
        //     }
        // }

        // stage('Build iOS') {
        //     steps {
        //         sh 'flutter build ios --release --no-codesign'
        //     }

        //     post {
        //         success {
        //             archiveArtifacts artifacts: 'build/ios/iphoneos/Runner.app', fingerprint: true
        //         }
        //     }
        // }
    }

    post {
        always {
            cleanUp()
        }
        success {
            echo 'Build successful!'
        }
        failure {
            echo 'Build failed!'
        }
    }
}

def cleanUp() {
    // sh 'flutter clean'
}